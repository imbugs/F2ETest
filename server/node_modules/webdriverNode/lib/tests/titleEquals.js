exports.command = function(expected, message, next )
{
	var self = this;

	self.title(
		function(result)
		{
            console.log('214556486456456');
			self.showTest(result.value === expected, result.value, expected, message);
            next();
		}
	);
	
};

